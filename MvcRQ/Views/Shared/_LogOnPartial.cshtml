@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink(RQResources.Views.Shared.SharedStrings.logoff, "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink(RQResources.Views.Shared.SharedStrings.logon, "LogOn", "Account") ]
}
